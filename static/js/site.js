document.addEventListener("DOMContentLoaded",()=>{const app=document.querySelector("#assessment-app");if(!app)return;const choices=[...app.querySelectorAll("[data-answer]")];choices.forEach(button=>button.addEventListener("click",()=>{choices.forEach(x=>x.setAttribute("aria-pressed","false"));button.setAttribute("aria-pressed","true");const p=document.querySelector("#progress");if(p)p.textContent="Selection saved locally — no per-question API call."; }));});