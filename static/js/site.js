document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".menu-toggle"),nav=document.querySelector("#primary-nav");if(toggle&&nav){toggle.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open));nav.classList.toggle("is-open",!open);});}const app=document.querySelector("#assessment-app");if(app){const choices=[...app.querySelectorAll("[data-answer]")];choices.forEach(button=>button.addEventListener("click",()=>{choices.forEach(x=>x.setAttribute("aria-pressed","false"));button.setAttribute("aria-pressed","true");const p=document.querySelector("#progress");if(p)p.textContent="Selection saved — continue when ready."; }));}});